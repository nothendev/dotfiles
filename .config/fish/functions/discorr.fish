function discorr
  exec /usr/bin/discord --enable-features=UseOzonePlatform --ozone-platform=wayland $argv
end

function operra
  exec /usr/bin/opera --enable-features=UseOzonePlatform --ozone-platform=wayland $argv
end
